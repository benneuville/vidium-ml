import {Template} from "../../language-server/generated/ast";
import {CompositeGeneratorNode} from "langium";

export function compileTemplate(template: Template, fileNode: CompositeGeneratorNode) {
    // Import required libraries
    return;
}