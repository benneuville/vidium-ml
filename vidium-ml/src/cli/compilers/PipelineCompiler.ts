import {Pipeline} from "../../language-server/generated/ast";
import {CompositeGeneratorNode} from "langium";

export function compilePipeline(pipeline: Pipeline, fileNode: CompositeGeneratorNode) {
    // Import required libraries
    return;
}